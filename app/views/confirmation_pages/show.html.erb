<div class="container container-new-page mt-4">
  <div class="row">
    <div class="col-6 form-wrapper mt-5">
      <h1>Thanks! You're Awesome</h1>
      <% saved_ingredients = 0 %>
      <% @orders.each do |order| %>
        <% saved_ingredients += order.ordered_amounts.count %>
      <% end %>
      <h2>You have saved <%= saved_ingredients %> ingredients from being thrown away!</h2>
      <br>
      <h3>You can pick up your bags at the following supermarkets:</h3>
      <% @orders.each do |order| %>
        <li><%= order.recipe.title%> @<%= order.recipe.supermarket.address%></li>
      <% end %>
      <br>
      <p>Please check you Mailbox! We have sent you a confirmation mail with a pick-up code to <em><%= @user.email %></em>!</p>
    </div>
    <div class="col-6" style="margin-top: 30px">
      <%= image_tag "order_confirmation.jpg", class: "img-new" %>
    </div>
  </div>

</div>
